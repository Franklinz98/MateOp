enum AssetOrientation { left, right }
enum AnimOrientation { vertical, horizontal }
enum AnimAlignment {
  topLeft,
  topCenter,
  topRight,
  centerLeft,
  centerRight,
  bottomLeft,
  bottomCenter,
  bottomRight,
}
