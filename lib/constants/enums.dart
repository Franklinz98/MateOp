enum AssetOrientation { left, right }
enum AnimOrientation { vertical, horizontal }
enum AnimAlignment {
  topLeft,
  topCenter,
  topRight,
  centerLeft,
  center,
  centerRight,
  bottomLeft,
  bottomCenter,
  bottomRight,
}
enum AnimType {
  parent,
  standalone,
}
enum OperationType {
  addition,
  subtraction,
  multiplication,
}
enum AuthScreen {
  login,
  recover,
}

enum MainScreen {
  home,
  ranking,
  about,
  options,
  loading,
}
